# QA report — 2026-04-27T061800Z

**Branch:** main
**HEAD:** 66dfc31 — fix(automation): ajv --spec=draft2020 + declare _banner — task lists now validate
**Diff size:** 0 tracked lines + 1 untracked file (empty `.gitkeep`)
**Threshold:** 1000 (default; no override)
**Invocation:** `/qa` interactive (Pass 1 + Pass 2 + Pass 3)

---

## Pass 1 — Claude self-report

### Summary
Materializes the `automation/qa-reports/` directory by adding an empty `.gitkeep` so the path that ADR-0013, STRUCTURE.md, and `automation/qa.sh` reference as the QA-report output destination actually exists in the repository. The directory is otherwise created lazily by `qa.sh` on first invocation; making it explicit means a contributor reading STRUCTURE.md sees the directory in `git ls-tree`, and Ralph's `automation/qa.sh` does not need to test `mkdir -p` on a path that's documented to exist.

### Files touched
| File | Change kind | One-sentence purpose |
|------|-------------|----------------------|
| `automation/qa-reports/.gitkeep` | A | Creates an empty file so git tracks the directory; the directory itself is referenced as the QA-report output path in ADR-0013 and STRUCTURE.md but had no on-disk presence. |

### Hard-rule compliance claims
- **AgentRunner-only model calls.** Applies? no. Compliant? n/a. Reason: the diff is one empty file; no `ai` package import surface is touched.
- **No JSON-as-storage.** Applies? no. Compliant? n/a. Reason: no JSON files added or modified; no storage-layer code changed.
- **TypeScript strict.** Applies? no. Compliant? n/a. Reason: no `.ts` files in the diff.
- **Do-not-lift list (ClaudeMap contracts/handlers/cache/enrichment/JSON-storage).** Applies? no. Compliant? n/a. Reason: no files lifted from `claudemap/`.
- **No second graph model.** Applies? no. Compliant? n/a. Reason: no orchestration / graph framework code touched.
- **Specs under `/spec/` are contracts (frontmatter required).** Applies? no. Compliant? n/a. Reason: nothing under `/spec/` changed.
- **Architectural decisions are ADRs.** Applies? unsure. Compliant? yes (I think). Reason: creating a documented directory is closer to "satisfying an ADR's stated layout" than a new architectural call. ADR-0013's References section explicitly cites `automation/qa-reports/` as the output directory; STRUCTURE.md inventories it; this commit just makes the on-disk state match the spec. No new ADR seems needed.
- **Never train against monitor signal.** Applies? no. Compliant? n/a. Reason: no fine-tune / training scripts touched.

### Decisions made that may need an ADR
- Whether `automation/qa-reports/` should be tracked at all. ADR-0013's "Costs" section already flagged a future pruning policy for the directory; this diff is consistent with that decision. I think no new ADR is required, but Pass 2 should verify.

### Uncertainty
- I'm using `.gitkeep` rather than `.gitignore`-with-`!` patterns. This is the convention already used elsewhere in the repo (`packages/skill/tests/.gitkeep`, `packages/skill/src/db/migrations/.gitkeep`, `tests/integration/suites/.gitkeep`).
- The `mkdir -p "$REPORTS_DIR"` line in `automation/qa.sh:117` becomes a no-op once the directory exists. It doesn't break anything but suggests the script author didn't expect the directory to be tracked. Pass 2 should call out a contradiction if it sees one.
- The diff is exactly one empty file. That is borderline trivial; a "no real audit possible" verdict from Pass 2 would be defensible.

---

## Pass 2 — Codex independent audit

> [codex]:
>
> ## 1. Self-report fidelity
> - The runtime-effect claim is overstated: the diff adds only `automation/qa-reports/.gitkeep`; it does not modify `automation/qa.sh`, so the script still runs `mkdir -p "$REPORTS_DIR"` unconditionally (`automation/qa-reports/.gitkeep`, `automation/qa.sh:114-115`).
> - The "makes the on-disk state match the spec" claim is inaccurate as written: after adding `automation/qa-reports/.gitkeep`, both referenced docs still say `qa-reports/` is "created on first run," so the repo state and docs now diverge (`automation/qa-reports/.gitkeep`, `STRUCTURE.md:72`, `docs/adr/0013-qa-self-audit-pattern.md:172`).
>
> ## 2. Hard-rule compliance
> - `AgentRunner-only model calls` — `not applicable`. The diff is confined to an empty placeholder in the QA report output directory and touches no model-call code path (`automation/qa-reports/.gitkeep`, `automation/qa.sh:10,114-118`).
> - `No JSON-as-storage` — `not applicable`. No JSON files or storage-layer code changed; the diff only adds the reports-directory placeholder (`automation/qa-reports/.gitkeep`, `automation/qa.sh:10,114-118`).
> - `TypeScript strict everywhere` — `not applicable`. No `.ts` or `.tsx` files are touched (`automation/qa-reports/.gitkeep`).
> - `Do-not-lift list (ClaudeMap contracts/handlers/cache/enrichment/JSON-storage)` — `not applicable`. No file under `claudemap/` or any lifted implementation file is in the diff (`automation/qa-reports/.gitkeep`).
> - `No second graph model` — `not applicable`. No orchestration or graph-runtime code changed; the existing QA gate contract in Ralph is untouched (`automation/qa-reports/.gitkeep`, `automation/ralph.sh:368-395`).
> - `Specs under /spec/ are contracts (frontmatter required)` — `not applicable`. Nothing under `/spec/` changed (`automation/qa-reports/.gitkeep`).
> - `Architectural decisions are ADRs` — `compliant`. I agree with the parent that this is not ADR-level: pre-creating an already-referenced output directory does not change the `/qa` protocol, Ralph exit-code mapping, dependencies, or substrate, though it does leave stale "created on first run" wording behind (`automation/qa.sh:114-118`, `automation/ralph.sh:368-395`, `docs/adr/0013-qa-self-audit-pattern.md:56,60,69,130,172`, `STRUCTURE.md:70-72`).
> - `Never train against monitor signal` — `not applicable`. No training, monitor, or model-routing code is touched by an empty `.gitkeep` addition (`automation/qa-reports/.gitkeep`).
>
> ## 3. Unrecorded architectural decisions
> - None. The only visible decision is to track the presence of an already-documented QA-report output directory with `.gitkeep`; that does not constrain future code, change the QA contract, introduce a dependency, or alter the substrate, and the parent did flag the "should this be tracked?" question for review (`automation/qa.sh:114-118`, `automation/ralph.sh:368-395`, `docs/adr/0013-qa-self-audit-pattern.md:130,136,172`, `STRUCTURE.md:70-72`).
>
> ## Findings table (severity-ranked)
>
> | ID | Severity | Area | Finding | File:line |
> |----|----------|------|---------|-----------|
> | F-01 | minor | docs | Adding tracked `automation/qa-reports/.gitkeep` makes the current "created on first run" wording stale in both the repo inventory and ADR-0013. | `STRUCTURE.md:72`, `docs/adr/0013-qa-self-audit-pattern.md:172` |
> | F-02 | nit | self-report fidelity | The self-report overstates the behavior change: `qa.sh` still unconditionally runs `mkdir -p "$REPORTS_DIR"`, so the diff only pre-creates the path; it does not remove the lazy-create step. | `automation/qa.sh:114-115` |
>
> COUNT_BLOCKER=0 COUNT_MAJOR=0 COUNT_MINOR=1 COUNT_NIT=1

---

## Pass 3 — Claude's response to Codex's findings

| ID | Codex finding | Claude action | Reason |
|----|---------------|---------------|--------|
| F-01 | "created on first run" wording in STRUCTURE.md:72 and ADR-0013:172 becomes stale once the directory is tracked. | accept-fix | Codex is right. Two options: (a) update STRUCTURE.md's wording to drop the "created on first run" phrase; (b) leave ADR-0013 alone because ADRs are immutable post-acceptance and supersede with a new ADR. ADR-0013 was just accepted in commit `eb0fd07`; per the project convention (CLAUDE.md "Hard rules" — *Architectural decisions are ADRs ... Numbered sequentially, immutable after acceptance, supersede with a new ADR*), I cannot edit ADR-0013. So: fix STRUCTURE.md only, leave ADR-0013 alone (the "created on first run" wording in 0013:172 is now historically accurate — it described state at the time of authorship). |
| F-02 | Self-report's claim that the diff means qa.sh "does not need to test mkdir -p" overstates the behavior change. | accept | Codex is right. The Pass-1 summary should have said "makes the directory visible in `git ls-tree` and to contributors reading STRUCTURE.md," not implied the script would change. The diff is exactly what F-02 describes: a pre-create, not a refactor. The Pass 1 self-report stays as-is (it's a record); future self-reports should be more careful. No code change for F-02. |

**Action plan for accepted findings.**
1. Edit `STRUCTURE.md:72` — change `qa-reports/                ← /qa and qa.sh report files (created on first run)` to `qa-reports/                ← /qa and qa.sh report files`.
2. Do NOT edit `docs/adr/0013-qa-self-audit-pattern.md`. ADR-0013 is Accepted; the "created on first run" wording was correct at authorship time. If this becomes load-bearing, supersede with a new ADR.

**Disagreements.** None. Both findings are accepted as Codex stated them.

---

## Outcome (auto-derived from Pass 2 COUNT line)

- Blockers: 0
- Majors: 0
- Minors: 1
- Nits: 1

**Recommended action for the human:**
- 0 blockers: commit is safe.
- 0 majors: no human review pause required.
- Minors and nits present: Action plan above addresses F-01 (one-line STRUCTURE.md edit) in the same commit as the `.gitkeep`. F-02 is a self-report quality issue, not a code change.

The Ralph autonomous loop applies the same severity → action mapping mechanically (see `automation/qa.sh` and ADR-0013).
