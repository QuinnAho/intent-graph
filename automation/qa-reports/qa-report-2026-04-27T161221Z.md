# QA report — 2026-04-27T16:12:21Z

**Branch:** main
**HEAD:** fc287ec — fix(automation): tighten ralph agent prompt and preserve per-attempt output transcripts
**Diff size:** 21 lines across 3 files
**Threshold:** 1000

---

## Pass 1 — Claude self-report

**Tier A** (diff ≤50 lines).

### Summary
Recovered work from a failed `p1-t01` Ralph attempt that hit `--max-turns 15` before emitting its completion signal. The agent had already correctly audited the Phase 1 stack pins against tech-spec.md and made two corrections, but ran out of turns before regenerating the lockfile. I regenerated the lockfile manually and re-ran `automation/verify.sh` (all required gates PASS) before staging for QA.

### Files touched
| File | Change kind | One-sentence purpose |
|------|-------------|----------------------|
| packages/skill/package.json | M | Add `fast-check ^3.23.0` to devDependencies — tech-spec Phase 1 calls for fast-check; was missing. |
| packages/webview/package.json | M | Bump `@xyflow/react` from `^12.3.6` to `^12.10.0` — tech-spec Phase 1 calls for xyflow v12+; the existing pin already satisfied that range, so the bump is a tightening, not a correction of a violation. |
| pnpm-lock.yaml | M | Regenerated to reflect the two package.json changes; pulls in `fast-check@3.23.2` + `pure-rand@6.1.0`, updates the resolved `@xyflow/react` to `12.10.2`. |

### Hard-rule applicability
- None of the eight hard rules apply directly. The diff touches no `.ts/.tsx` files, no `/spec/` markdown, no ADRs, no AgentRunner code, no model-call sites, no SQLite schema, no graph model, no `claudemap/` lifts. The changes are dependency-pin adjustments + lockfile regen, scoped to phase-1 task `p1-t01` (`Confirm scaffolding pins match Phase 1 stack decisions`).
- Adjacent concern: this work was done by a Ralph agent that did not emit its completion signal, so the loop's normal post-task commit pathway never ran. Committing it manually under `/qa` is the documented recovery — the audit gate still runs against the diff.

### Decisions / uncertainty
- The agent's xyflow bump from `^12.3.6` to `^12.10.0` is a tightening, not a violation fix. I think it's defensible (the task spec asks to "verify each pin matches the tech-spec call"; the spec says "v12+", so any v12 satisfies it). Codex may reasonably flag this as out-of-scope churn. If so, the right fix is to revert the xyflow bump and keep only the fast-check addition.
- I'm uncertain whether the lockfile diff (`pure-rand@6.1.0` transitively pulled in by `fast-check`) needs explicit mention in a commit message or whether silent transitive resolution is fine.

---

## Pass 2 — Codex independent audit

> [codex]:
>
> ## 1. Self-report fidelity
> - The self-report mischaracterizes the `@xyflow/react` change as optional tightening: the diff raises the pin from `^12.3.6` to `^12.10.0`, and Phase 1 of `tech-spec.md` requires `@xyflow/react` `v12.10+`, so this is a required spec-alignment fix, not out-of-scope churn (`packages/webview/package.json:19`, `pnpm-lock.yaml:180`, `pnpm-lock.yaml:181`).
>
> ## 2. Hard-rule compliance
> - AgentRunner-only model calls: `not applicable` — the diff only changes dependency manifests and lockfile entries, with no source imports or model-call sites touched (`packages/skill/package.json:40`, `packages/webview/package.json:19`, `pnpm-lock.yaml:150`, `pnpm-lock.yaml:181`).
> - No JSON-as-storage: `not applicable` — `package.json` and `pnpm-lock.yaml` are build metadata, and the diff does not touch runtime persistence or storage code (`packages/skill/package.json:40`, `packages/webview/package.json:19`, `pnpm-lock.yaml:150`, `pnpm-lock.yaml:181`).
> - Do not lift from `claudemap/{contracts/, handlers/, cache layer, enrichment pipeline, JSON storage}`: `not applicable` — no `claudemap/` files, provenance headers, or `LIFT_LOG.md` entries are changed; the diff is confined to package manifests and lockfile (`packages/skill/package.json:40`, `packages/webview/package.json:19`, `pnpm-lock.yaml:150`, `pnpm-lock.yaml:181`).
> - TypeScript strict everywhere: `not applicable` — no `.ts` or `.tsx` files are changed, so the diff introduces no `any` usage or `// @ts-ignore` comments (`packages/skill/package.json:40`, `packages/webview/package.json:19`, `pnpm-lock.yaml:150`, `pnpm-lock.yaml:181`).
> - No second graph model: `not applicable` — the diff does not alter orchestration, graph-state, or task-model code; it only adjusts dependency pins (`packages/skill/package.json:40`, `packages/webview/package.json:19`).
> - Specs under `/spec/` are contracts: `not applicable` — no `/spec/` markdown files are touched (`packages/skill/package.json:40`, `packages/webview/package.json:19`, `pnpm-lock.yaml:150`).
> - Architectural decisions are ADRs under `/docs/adr/`: `not applicable` — no ADR files are added or modified in this diff (`packages/skill/package.json:40`, `packages/webview/package.json:19`, `pnpm-lock.yaml:150`).
> - Never train any IntentGraph component against the monitor LLM's signal: `not applicable` — the diff does not touch monitor, training, or evaluation codepaths (`packages/skill/package.json:40`, `packages/webview/package.json:19`).
>
> ## 3. Unrecorded architectural decisions
> - No unrecorded architectural decisions. The visible dependency decisions are `fast-check` for the skill package and `@xyflow/react` `^12.10.0` for the webview, and both are already recorded in existing architecture docs rather than newly introduced here (`packages/skill/package.json:40`, `packages/webview/package.json:19`, `pnpm-lock.yaml:150`, `pnpm-lock.yaml:181`).
>
> ## Findings table (severity-ranked)
>
> | ID | Severity | Area | Finding | File:line |
> |----|----------|------|---------|-----------|
> | F-01 | minor | dependency pinning | The self-report incorrectly treats the `@xyflow/react` bump as optional tightening; Phase 1 requires `v12.10+`, so reverting this change would undo a real spec-alignment fix. | packages/webview/package.json:19 |
>
> COUNT_BLOCKER=0 COUNT_MAJOR=0 COUNT_MINOR=1 COUNT_NIT=0

---

## Pass 3 — Claude's response to Codex's findings

| ID | Codex finding | Claude action | Reason |
|----|---------------|---------------|--------|
| F-01 | Self-report mischaracterized the `@xyflow/react` bump as optional tightening; tech-spec actually requires `v12.10+`, making this a spec-alignment fix. | accept-fix | Verified against `tech-spec.md:436` (`pin '@xyflow/react' v12.10+, 'elkjs', 'zustand'`). I had read "v12+" from memory of the high-level pillars; the Phase 1 entry is more specific. The previous pin `^12.3.6` would have satisfied install of any 12.3.x — that's a violation of the spec floor, not just looser-than-necessary. The agent's bump is correct; my self-report's framing was wrong. |

**Action plan for accepted findings.**
1. Keep the diff as-is. Both edits (`fast-check ^3.23.0` add, `@xyflow/react ^12.10.0` bump) are required Phase 1 spec-alignment fixes.
2. Use a commit subject and body that name the fix as spec alignment, not "tightening." Cite `tech-spec.md` line 436 for the xyflow floor.

**Disagreements.** None.

---

## Outcome (auto-derived from Pass 2 COUNT line)

- Blockers: 0  Majors: 0  Minors: 1  Nits: 0

**Recommended action for the human:**
- Only minors/nits: commit is safe; address inline if quick.

The minor was a Pass-1 framing error, not a code issue. It's resolved by the Pass-3 action plan (rewriting the commit message with the correct framing). No source-file change needed.
