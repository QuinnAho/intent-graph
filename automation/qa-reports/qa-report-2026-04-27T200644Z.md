# QA report — 2026-04-27T20:06:44Z

> **STALE — kept as audit trail.** All four findings below were addressed before commit: F-01 (event_log BLOB columns), F-02 (ADR-0017 drafted to record the task-state-machine split), F-03 (task_active view + vec0 sql constants + row_audit trigger contract added to schema.ts), F-04 (structured Zod transforms for tool_calls/monitor_verdict/usage/seed_node_ids/ppr_top_k/filters_passed). The post-fix audit is qa-report-2026-04-27T203032Z.md (0 majors / 1 minor — the minor flagged a code-comment vs SELECT-body mismatch, which was tightened before commit). Do not read the findings below as the current state.


**Branch:** main
**HEAD:** 6774068 — chore(automation): approve phase-2-static-graph task list with adr-0015/0016 cleared and architect revisions applied
**Diff size:** 818 lines across 13 files
**Threshold:** 1000

---

## Pass 1 — Claude self-report

**Tier C** (501–1000 lines, full + extra honesty).

I cannot promise calibration on a diff this size. Treat my self-report as best-effort and lean harder on Pass 2.

### Summary
This is the recovered p2-t01 deliverable: the full ten-table SQLite schema from tech-spec §4 landing as both Drizzle schema-as-TS (packages/skill/src/db/schema.ts) and Zod application-layer validation (packages/shared/src/schemas/*.ts) plus derived TS types (packages/shared/src/types/*.ts). Per ADR-0015 (Accepted 2026-04-27), all ten tables ship monolithically.

The original ralph attempt at p2-t01 hit max-turns three consecutive times and aborted. Most of the Zod-side work was done by the agent before each abort; I corrected one architectural drift in the agent's output (split TaskStatus into storage + view enums), wrote the missing Drizzle DDL by hand, fixed two TS errors in the self-referential FK declarations, and ran the full verify gate (typecheck/lint/test PASS; migrate:lint FAIL because atlas CLI not installed locally — runs in CI).

### Hard-rule applicability and compliance

| Rule | Applies | Compliant | Reason |
|------|---------|-----------|--------|
| AgentRunner-only model calls | does not apply | n/a | No model-call sites; no `ai` imports. |
| No JSON-as-storage | applies | compliant | The diff DEFINES the SQLite substrate that replaces JSON-as-storage. |
| TypeScript strict | applies | compliant (I think) | No `any`, no `// @ts-ignore`. `AnySQLiteColumn` is the documented Drizzle self-FK pattern. |
| Do-not-lift list | does not apply | n/a | Nothing under claudemap/ is touched. |
| No second graph model | applies | compliant | This diff IS the graph model. |
| Specs under /spec/ | does not apply | n/a | No /spec/*.md files touched. |
| Architectural decisions are ADRs | applies | compliant (I claimed) | ADR-0015 cited in schema.ts; ADR-0016 cited in obligation.ts. (Codex disagreed — see Pass 3.) |
| Never train against monitor signal | does not apply | n/a | No model-training code. |

### Decisions / uncertainty

1. **§4.1/§4.5 status-enum reconciliation.** Tech-spec §4.1's task body status enum lists 7 values; §4.5's task_active view filters on 5 including `monitor_pending`. I split into `TaskStatusSchema` (7, storage) and `TaskActiveStatusSchema` (5, view-only). Proceeded with option 3 from earlier discussion without explicit human pick.
2. **Drizzle conventions vs §4 literal DDL.** STRICT, WITHOUT ROWID, partial-index WHERE, JSON-extract index, AFTER triggers, vec0 virtuals, and the task_active VIEW are all things I deferred to "the migration emits raw SQL in p2-t02" — trust-the-convention concern flagged.
3. **`pnpm migrate:lint` failed** because atlas CLI not installed locally. CI runs it. No migrations exist yet.
4. **Tier C honesty:** sanity-checked types/lease.ts re-exports LeaseScope (it does); other type files re-export their respective enum aliases too.

---

## Pass 2 — Codex independent audit

> [codex]:
>
> ## 1. Self-report fidelity
> - The opening claim that the "full ten-table SQLite schema" landed is overstated: the diff does not model `task_active` as a view object and does not model `vec_intent` / `vec_code` as tables; those are deferred to comments and string constants for later raw SQL work instead [packages/skill/src/db/schema.ts:175-177, 294-302].
> - The self-report omits a substantive substrate drift: `event_log.payload`, `prev_hash`, and `hash` are implemented as text/json in Drizzle, while the same diff's shared row schema documents them as raw byte arrays (`Uint8Array`) for BLOB storage [packages/skill/src/db/schema.ts:181-196; packages/shared/src/schemas/trace.ts:97-108].
> - "Zod application-layer validation" is too broad a description of what landed: several JSON-bearing row fields are still only opaque strings in the row schemas (`filters_passed`, `tool_calls`, `monitor_verdict`, `usage`, `seed_node_ids`, `ppr_top_k`) rather than validated structured payloads [packages/shared/src/schemas/obligation.ts:23-33; packages/shared/src/schemas/trace.ts:79-86, 116-123].
>
> ## 2. Hard-rule compliance
> - `AgentRunner-only model calls`: `compliant` — the touched files import `zod` and `drizzle`, and the diff introduces no `ai` imports at all [packages/shared/src/schemas/node.ts:7; packages/shared/src/schemas/trace.ts:7; packages/skill/src/db/schema.ts:24-34].
> - `No JSON-as-storage`: `compliant` — the diff adds SQLite schema and row-validation code, not JSON-file-backed storage; JSON appears only as column payload inside SQLite [packages/skill/src/db/schema.ts:39-302].
> - `Do not lift from claudemap`: `not applicable` — no `claudemap/` files, provenance headers, or `LIFT_LOG.md` entries are touched in this diff [N/A].
> - `TypeScript strict everywhere`: `compliant` — the diff introduces no `any` and no `// @ts-ignore`; the loosest types are `unknown` and Drizzle's `AnySQLiteColumn` [packages/shared/src/schemas/node.ts:130; packages/skill/src/db/schema.ts:31-34, 61, 194, 212].
> - `No second graph model`: `compliant` — the changes stay inside the relational/Zod substrate and do not add any alternate graph or orchestration runtime [packages/skill/src/db/schema.ts:1-22, 39-302].
> - `Specs under /spec/ are contracts`: `not applicable` — no `/spec/` files are modified here [N/A].
> - `Architectural decisions are ADRs under /docs/adr/`: `non-compliant` — the diff makes load-bearing choices about task-state semantics and the canonical `event_log` representation without any accompanying ADR change [packages/shared/src/schemas/node.ts:97-123; packages/skill/src/db/schema.ts:181-196].
> - `Never train any IntentGraph component against the monitor LLM's signal`: `not applicable` — the diff adds monitor/trace storage shapes only, with no training or fine-tuning path [packages/shared/src/schemas/trace.ts:38-46, 66-95].
>
> ## 3. Unrecorded architectural decisions
> - The diff changes the canonical `event_log` substrate from the spec's byte-oriented BLOB fields to text/json columns (`payload`, `prev_hash`, `hash`) [packages/skill/src/db/schema.ts:181-196]. This is load-bearing because `event_log` is the canonical, hash-chained source of truth. ADR currently records it: no.
> - The diff resolves the spec's task-status conflict by persisting `produced` in `TaskStatusSchema` and treating `monitor_pending` as a view-only `TaskActiveStatusSchema` value [packages/shared/src/schemas/node.ts:97-123; packages/shared/src/schemas/task.ts:9-16]. This is load-bearing because it fixes the task state machine across storage, scheduling, and MCP surfaces. ADR currently records it: no.
> - The diff redefines "monolithic schema landed" to mean "comments/constants now, raw SQL later" for `task_active`, vec0 tables, and other non-builder DDL pieces [packages/skill/src/db/schema.ts:15-22, 175-177, 294-302]. This is load-bearing because it changes ADR-0015's promised day-1 schema/migration scope. ADR currently records it: no; ADR-0015 says the initial migration covers the whole schema and view [docs/adr/0015-schema-scope-staged-vs-monolithic.md:26-29].
>
> ## Findings table (severity-ranked)
>
> | ID | Severity | Area | Finding | File:line |
> |----|----------|------|---------|-----------|
> | F-01 | blocker | substrate | `event_log.payload`, `prev_hash`, and `hash` are modeled as text/json instead of the spec's canonical BLOB bytes, and that substrate change is not ADR-backed. | packages/skill/src/db/schema.ts:194 |
> | F-02 | blocker | task-state | The diff resolves the task-status contract by making `monitor_pending` view-only and persisting `produced`, but no ADR records that state-machine decision. | packages/shared/src/schemas/node.ts:97 |
> | F-03 | major | schema-scope | `task_active` and the vec0 tables are not actually modeled here; they are deferred to future raw SQL/comments, so the claimed monolithic §4 schema is incomplete. | packages/skill/src/db/schema.ts:175 |
> | F-04 | minor | validation | Several JSON-bearing row fields remain opaque strings, so shared schemas are not yet providing structured validation for those columns. | packages/shared/src/schemas/trace.ts:80 |
>
> COUNT_BLOCKER=2 COUNT_MAJOR=1 COUNT_MINOR=1 COUNT_NIT=0

---

## Pass 3 — Claude's response to Codex's findings

| ID | Codex finding | Claude action | Reason |
|----|---------------|---------------|--------|
| F-01 | event_log.payload/prev_hash/hash wrong type (text/json instead of BLOB) | accept-fix | Tech-spec §4.6 literally says `BLOB NOT NULL`. The Zod schema (trace.ts:97-108) correctly uses `z.instanceof(Uint8Array)` but my Drizzle column types contradict it. Hash-chain integrity depends on byte-exact `prev_hash \|\| canonical(payload)` SHA-256, which JSON-text storage breaks. Drizzle has `blob('col', { mode: 'buffer' })` — I just used the wrong column type. |
| F-02 | Task-state machine reconciliation needs an ADR | accept-fix | The §4.1/§4.5 status enum split is real architectural work; option 3 (split storage from view enum) constrains the orchestrator + scheduler + MCP surfaces. A future contributor reading just the code won't know which way the wind blew. ADR-0017 needed before commit. |
| F-03 | task_active VIEW + vec0 virtuals not modeled in schema.ts | accept-fix | Re-read ADR-0015:26-29 — it explicitly says "all ten table definitions **and the view**", "vec_intent / vec_code virtual tables are **declared**", and "AFTER triggers for row_audit are part of the initial migration." My header-comment "the migration emits raw SQL" defers to a migration that doesn't exist yet, and even when it does (p2-t02), drizzle-kit generates it *from this file*. I need to add the raw-SQL artifacts to schema.ts using Drizzle's `sql\`\`` template + `view()` builder. |
| F-04 | JSON-bearing string columns lack structured Zod validation | accept-fix | filters_passed (string[]), tool_calls (TraceToolCall[]), monitor_verdict (MonitorVerdict), usage (TraceUsage), seed_node_ids ({node_id, similarity}[]), ppr_top_k ({node_id, score}[]) all have structured shapes elsewhere in the file or §4. Wire them up with `z.string().transform(JSON.parse).pipe(<inner schema>)` or equivalent. |

**Action plan for accepted findings.**

1. **F-01:** In `packages/skill/src/db/schema.ts:181-196`, change `payload`, `prevHash`, and `hash` from `text(..., {mode: 'json'})` / `text(...)` to `blob('payload', {mode: 'buffer'}).notNull()` / `blob('prev_hash', {mode: 'buffer'})` / `blob('hash', {mode: 'buffer'}).notNull()`. Add a one-line note in the schema.ts header comment about hash-chain byte-exactness.

2. **F-02:** Draft `docs/adr/0017-task-state-machine.md` (Status: Proposed) covering: (a) why §4.1 and §4.5 conflict; (b) the three options (widen storage, narrow storage / fix view, split storage from view enum); (c) the chosen split (option 3); (d) the consequences (orchestrator must compute `monitor_pending` from observed state, persisted enum stays at 7 values, MCP `graph.upsert_node` accepts only the storage enum). Cite tech-spec §4.1 and §4.5. Use the same Status: Proposed pattern as ADR-0015/0016 — accept after human review.

3. **F-03:** Extend `packages/skill/src/db/schema.ts` to model:
   - The `task_active` view as a Drizzle `sqliteView('task_active', ...)` with the §4.5 SELECT body.
   - The vec0 virtual tables: since Drizzle does not have a `virtualTable` builder, use a `sql\`CREATE VIRTUAL TABLE IF NOT EXISTS vec_intent USING vec0(...)\`` exported as a constant the migration callback runs at apply-time.
   - The AFTER triggers for row_audit on tracked tables (node, edge, obligation, lease).
   - The JSON-extract index `te_flagged ON trace_event(json_extract(monitor_verdict, '$.flagged'))`.
   - The partial-index WHERE on `edge_unique` (already done correctly).
   - Add a `STRICT` / `WITHOUT ROWID` table-options helper if Drizzle exposes one; otherwise, add a migration-callback comment that p2-t02 must emit them.

4. **F-04:** In `packages/shared/src/schemas/`, replace opaque-string row-fields with `z.string().transform(<parse>).pipe(<inner>)`:
   - `obligation.filters_passed` → `z.array(z.string())`
   - `trace_event.tool_calls` → `z.array(TraceToolCallSchema).nullable()`
   - `trace_event.monitor_verdict` → `MonitorVerdictSchema.nullable()`
   - `trace_event.usage` → `TraceUsageSchema`
   - `trace_event.retrieved_node_ids` / `produced_mutations` / `verifier_outcomes` / `probe_results` → keep as opaque strings for now BUT document the deferred validation in a comment (these are JSON shapes the spec doesn't fully define — not safe to invent here).
   - `retrieval.seed_node_ids` → `z.array(z.object({ node_id: z.string(), similarity: z.number() }))`
   - `retrieval.ppr_top_k` → `z.array(z.object({ node_id: z.string(), score: z.number() }))`

**Disagreements.** None. All four findings are evidence-backed against tech-spec or ADR-0015.

---

## Outcome (auto-derived from Pass 2 COUNT line)

- Blockers: 2  Majors: 1  Minors: 1  Nits: 0

**Recommended action for the human:**
- Blockers > 0: do not commit. Fix and re-run `/qa`.

The two blockers are real substrate issues (BLOB-vs-text on the hash chain, and an unrecorded architectural decision on the task state machine). The major is an honest gap between ADR-0015's promise and what landed. All four findings are accept-fix; nothing is pushback. Action plan above.
