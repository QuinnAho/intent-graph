# QA report — 2026-04-27T211243Z

**Branch:** main
**HEAD:** 90ffd9a — chore(automation): raise phase-2 max_iterations_per_task from 15 to 30
**Diff size:** 1374 lines
**Threshold:** 1500
**Invocation:** automation/qa.sh (Pass-2-only, bash-loop entrypoint)
**Ralph task:** p2-t02 (see automation/tasks/phase-2-static-graph/tasks.json)
**Codex duration:** 351s, exit 0

---

## Pass 1 — Claude self-report

N/A — automation/qa.sh runs Pass-2-only because the bash loop has no Claude session to produce a self-report. See .claude/commands/qa.md for the full two-pass flow used by interactive `/qa` runs and ADR-0013 for the rationale.

---

## Pass 2 — Codex independent audit

> [codex]:
>
> ## 1. Self-report fidelity
> N/A — Pass-2-only invocation from automation/qa.sh.
> 
> ## 2. Hard-rule compliance
> - `AgentRunner-only model calls`: compliant — the diff only adds a SQL migration and Drizzle metadata under `packages/skill/src/db/migrations/`, with no TypeScript source and no `from 'ai'` imports (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:1`, `packages/skill/src/db/migrations/meta/0000_snapshot.json:1`, `packages/skill/src/db/migrations/meta/_journal.json:1`).
> - `No JSON-as-storage`: non-compliant — the migration makes JSON part of runtime storage semantics by reading task state from `node.body` with `json_extract`, storing JSON-like payloads in trace/retrieval columns, and persisting JSON row snapshots in `row_audit` triggers, so JSON is not export-only here (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:55`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:75`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:89-90`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:101-109`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:126-147`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:167-179`).
> - `Do not lift from claudemap/{contracts/, handlers/, cache layer, enrichment pipeline, JSON storage}`: not applicable — the diff only adds new migration artifacts and does not touch `claudemap/`, provenance headers, or `LIFT_LOG.md` (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:1`, `packages/skill/src/db/migrations/meta/0000_snapshot.json:1`).
> - `TypeScript strict everywhere`: not applicable — no `.ts` files are added or modified, so this diff introduces no `any` or `// @ts-ignore` surface to evaluate (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:1`, `packages/skill/src/db/migrations/meta/0000_snapshot.json:1`).
> - `No second graph model`: compliant — the schema stays inside the SQLite substrate with `node`, `edge`, `event_log`, `trace_event`, and a derived `task_active` view, and it does not introduce another graph/orchestration system (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:1`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:17`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:112`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:147`).
> - `Specs under /spec/ are contracts`: not applicable — no files under `/spec/` are added or modified in this diff (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:1`, `packages/skill/src/db/migrations/meta/0000_snapshot.json:1`).
> - `Architectural decisions are ADRs under /docs/adr/`: non-compliant — the diff makes load-bearing persistence/bootstrap decisions in schema comments and DDL, but adds no ADR file alongside them, including deferring `vec0` DDL out of the baseline migration and sourcing task state from JSON in `node.body` (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:147`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:149-161`).
> - `Never train any IntentGraph component against the monitor LLM's signal`: compliant — the diff stores monitor-related artifacts for tracing but adds no training, fine-tuning, or feedback loop from `monitor_verdict`/`probe_results` into model behavior (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:130-131`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:146`).
> 
> ## 3. Unrecorded architectural decisions
> - The migration decides that `vec0` schema objects are not created by the baseline migration file and are instead applied later by runtime code after `sqlite-vec` loads (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:149-161`). This is load-bearing because it changes bootstrap order and makes runtime code part of schema application rather than keeping the migration self-contained. No ADR is added in this diff; the comment cites ADR-0015, but the reinterpretation itself is not recorded as an ADR change.
> - The migration decides that task lifecycle state and several trace/audit collections live as JSON text inside relational rows and are queried with SQLite JSON functions (`packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:55`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:75`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:89-90`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:101-109`, `packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:126-147`). This is load-bearing because it fixes the persistence contract for tasks, retrieval, traces, and CDC snapshots and makes JSON a runtime storage dependency. No ADR is added in this diff; whether an older ADR already records this cannot be determined from the diff alone.
> 
> ## Findings table (severity-ranked)
> 
> | ID | Severity | Area | Finding | File:line |
> |----|----------|------|---------|-----------|
> | F-01 | blocker | storage-contract | The migration violates the no-JSON-as-storage rule by making JSON in `node.body`, `row_audit`, and trace/retrieval text columns part of the runtime source of truth. | packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:55,75,89-90,101-109,126-147,167-179 |
> | F-02 | major | migration-contract | The baseline migration intentionally omits `vec0` DDL and reassigns schema application to runtime SQL constants after extension load, which is a load-bearing bootstrap change with no ADR added in this diff. | packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:149-161 |
> | F-03 | major | task-model | Active-task state is encoded inside `node.body` JSON and projected through `task_active` rather than modeled as first-class relational columns, and no ADR accompanies that persistence-model decision in this diff. | packages/skill/src/db/migrations/0000_wonderful_dorian_gray.sql:55,147 |
> 
> COUNT_BLOCKER=1 COUNT_MAJOR=2 COUNT_MINOR=0 COUNT_NIT=0

---

## Pass 3 — automated outcome

- Blockers: 1
- Majors: 2
- Minors: 0
- Nits: 0

**Severity → action mapping (applied by ralph.sh):**
- Blockers > 0 → task is marked blocked; loop continues with next task.
- Majors > 0 → loop halts for human review.
- Only minors / nits → loop proceeds to commit.

