# QA report — 2026-04-28T02:09:12Z (retroactive audit of HEAD~3..HEAD)

**Branch:** main
**HEAD:** 167cf9e — ralph(p2-t04): /spec/*.md frontmatter parser → graph nodes (intents, constraints, decisions, concepts)
**Diff range:** HEAD~3..HEAD (commits 03ba175, 4a65b86, 167cf9e — p2-t02, p2-t03, p2-t04)
**Diff size:** 2474 lines across 18 files
**Threshold:** 1500 (raised from 1000 default for the multi-commit retro audit)
**Invocation:** manual (qa.sh runs against uncommitted work; retroactive audit built the prompt manually from automation/qa-prompt.template.md)

---

## Pass 1 — Claude self-report

Pass-1 self-report archived at automation/sessions/retro-qa-self-report.md.

---

## Pass 2 — Codex independent audit

> [codex]:
>
> ## 1. Self-report fidelity
> - The self-report overstates contract-gate coverage for the new concept fixture: the diff makes `concepts/` a first-class parsed spec kind (`packages/skill/src/parser/spec-md/index.ts:20-24,99-103`) and even says the validator enforces its four required fields (`spec/concepts/README.md:38`), but no validator change appears anywhere in `HEAD~3..HEAD`, so this range does not show `pnpm check:agent-config` actually covering the new concept kind.
> - The self-report describes `js-yaml` as "pre-authorized," but the audited diff only shows a new runtime dependency and parser implementation (`packages/skill/package.json:34,42`, `packages/skill/src/parser/spec-md/frontmatter.ts:10`) and no ADR addition in the audited range recording that dependency choice.
>
> ## 2. Hard-rule compliance
> - `AgentRunner-only model calls`: compliant — the audited additions are DB/bootstrap/parser code importing `better-sqlite3`, `drizzle-orm`, `sqlite-vec`, `js-yaml`, and `zod`, with no `ai` helper imports outside AgentRunner.
> - `No JSON-as-storage`: compliant — the diff keeps SQLite as the substrate and uses JSON only inside SQLite columns/expressions.
> - `Do not lift from claudemap/...`: compliant — the audited range only touches `automation/`, `packages/skill/`, `spec/`, and lock/package metadata.
> - `TypeScript strict everywhere`: compliant — explicit interfaces/Zod schemas; no `any` or `// @ts-ignore`.
> - `No second graph model`: compliant — parser emits inserts for the existing `node`/`edge` substrate; DB init only boots the existing schema.
> - `Specs under /spec/ are contracts`: **non-compliant** — the diff promotes `concepts/` into the parsed spec surface and claims validator enforcement, but the audited range never updates the contract gate.
> - `Architectural decisions are ADRs under /docs/adr/`: **non-compliant** — load-bearing choices without ADRs in the diff: runtime vec0 DDL application (vs migration-time per ADR-0015:28), js-yaml dependency adoption.
> - `Never train against monitor signal`: compliant — only stores monitor artifacts.
>
> ## 3. Unrecorded architectural decisions
> - Vec0 tables now created in runtime bootstrap after `sqliteVec.load()` rather than by the baseline SQL migration. Load-bearing; ADR-0015 says the opposite.
> - `js-yaml` is now a runtime dependency of the skill parser. Load-bearing; existing ADRs only describe js-yaml as a future follow-up.
> - `parent` frontmatter deterministically emits a `realizes` edge. Load-bearing for Coverage Verifier inputs; no current ADR records the mapping.
>
> ## Findings table (severity-ranked)
>
> | ID | Severity | Area | Finding | File:line |
> |----|----------|------|---------|-----------|
> | F-01 | blocker | spec-contract | The diff makes `spec/concepts/` a parser-consumed spec kind and documents validator enforcement for it, but never extends the contract gate, so `/spec/concepts/*.md` is outside the required frontmatter enforcement. | `packages/skill/src/parser/spec-md/index.ts:20-24,99-103`; `spec/concepts/README.md:38` |
> | F-02 | major | bootstrap-contract | Vec0 DDL was moved out of the baseline migration and into runtime bootstrap after `sqlite-vec` load, which changes the DB-init/migration contract without a superseding ADR. | `packages/skill/src/db/init.ts:126-139`; `packages/skill/src/db/schema.ts:336-340` |
> | F-03 | major | dependency-governance | The skill now depends on `js-yaml` for spec ingestion, but no ADR in the audited range records or ratifies that new dependency choice. | `packages/skill/package.json:34,42`; `packages/skill/src/parser/spec-md/frontmatter.ts:10` |
> | F-04 | major | graph-semantics | The parser now turns `parent` into a `realizes` edge for parsed spec files, which fixes graph semantics without an ADR or spec citation that actually records that mapping. | `packages/skill/src/parser/spec-md/index.ts:157-160`; `spec/concepts/README.md:29` |
> | F-05 | minor | audit-trail | `qa-report-2026-04-27T211243Z.md` is committed without the stale/audit-trail annotation used on the earlier report and still presents blocker/major claims as current, which will mislead the next reviewer. | `automation/qa-reports/qa-report-2026-04-27T211243Z.md:28,33,44-46` |
>
> COUNT_BLOCKER=1 COUNT_MAJOR=3 COUNT_MINOR=1 COUNT_NIT=0

---

## Pass 3 — Claude's response to Codex's findings

| ID | Codex finding | Claude action | Reason |
|----|---------------|---------------|--------|
| F-01 | spec/concepts/ not in CI validator | accept-fix (this session) | scripts/check-agent-config.ts walks intents/, constraints/, decisions/ but not concepts/. Real gap; ~3-line fix. Doing now. |
| F-02 | Vec0 runtime bootstrap reinterprets ADR-0015:28 | accept-fix (defer to next session, ADR draft) | This is a real architectural amendment — ADR-0015:28 said "DDL in the initial migration" and I moved it to runtime apply because SQLite parses CREATE VIRTUAL TABLE at parse time. The reinterpretation is defensible (ADR-0015:28 also says "extension load is gated by phase-5 wiring") but Codex is right that "in spirit" reinterpretation belongs in a new ADR, not a code comment. Drafting ADR-0018 next session. |
| F-03 | js-yaml dependency without ADR-in-range | accept-fix (defer to next session, lightweight) | Disagree on severity but accept the fix. ADR-0006 #3 explicitly pre-authorized this dependency by name ("when the spec frontmatter schema grows, replace the scanner with `js-yaml` or similar"). The pre-authorization IS the ADR. Codex's literal point — "no ADR addition in the audited range" — is true but conflates "no new ADR" with "no governing ADR." Right resolution: add a one-line note to ADR-0006 marking #3 as activated by p2-t04 (167cf9e). |
| F-04 | parent → realizes edge mapping has no ADR | accept-fix (defer to next session, ADR draft) | The mapping is implicit in spec/intents/README.md (the `parent` field exists) and in §4.2's edge-kind enum (realizes is one of nine), but Codex is right that no document explicitly says "parent in YAML frontmatter projects to a realizes edge in the graph." This is load-bearing for Coverage Verifier inputs (p2-t07) and drift detection (phase 4). Drafting ADR-0019 next session. |
| F-05 | qa-report-2026-04-27T211243Z.md not annotated as STALE | accept-fix (this session) | One-line annotation. Doing now. |

**Action plan for accepted findings.**

**This session (F-01, F-05):**
1. F-01: Update scripts/check-agent-config.ts to walk spec/concepts/ alongside the other three kinds. Run pnpm check:agent-config to verify it passes against the example fixtures.
2. F-05: Annotate qa-report-2026-04-27T211243Z.md with a STALE banner explaining when its findings were resolved.

**Next session (F-02, F-03, F-04):**
3. F-02: Draft `docs/adr/0018-vec0-runtime-bootstrap.md` (Status: Proposed) amending ADR-0015:28. Decision: vec0 CREATE VIRTUAL TABLE statements are emitted by the runtime extension-load path in db/init.ts, NOT by the baseline SQL migration. Reasoning: SQLite parses CREATE VIRTUAL TABLE ... USING vec0 at parse time and requires the module to be registered before parsing; IF NOT EXISTS does not avoid the lookup. ADR-0015:28's literal reading would crash bootstrap on every fresh DB until phase-5 wires the extension. The runtime-apply path preserves ADR-0015:28's intent ("DDL is owned by the schema layer, extension load is gated by phase 5") at the cost of one bootstrap-time SQL execution that lives in db/init.ts rather than the migration file.
4. F-03: Append a one-line activation note to ADR-0006 #3 citing commit 167cf9e (p2-t04) as the activation point. Or, if cleaner, draft a tiny ADR-0020 linking back to ADR-0006 #3. Decide once F-02 is drafted (single ADR amending two might be cleaner if F-04's mapping decision lives there too).
5. F-04: Draft `docs/adr/0019-spec-md-parent-realizes.md` (Status: Proposed) recording the mapping: `parent: <node-id>` in /spec/*.md frontmatter projects to an `edge.kind = 'realizes'` from the spec node to the parent. Decision is implementation-already-shipped; ADR records the contract so future drift detection (phase 4) and Coverage Verifier (p2-t07) can rely on it.

**Disagreements.** F-03 severity. ADR-0006 #3 is the governing ADR for the js-yaml choice; the audited range failing to *add* an ADR doesn't mean the choice is *unrecorded*. Practical resolution still accepts the fix (it costs almost nothing to add the activation note) but the framing is weaker than Codex's reading would suggest.

---

## Outcome (auto-derived from Pass 2 COUNT line)

- Blockers: 1  Majors: 3  Minors: 1  Nits: 0

**Recommended action for the human:**
- Blockers > 0: do not merge or treat as accepted. F-01 is being fixed in this session; F-02/F-03/F-04 are deferred to next session as ADR drafts. The work itself is committed but not yet "closed" — open ADR drafts in the next session before resuming p2-t05 implementation.
