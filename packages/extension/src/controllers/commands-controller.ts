// Registers all `intentgraph.*` VS Code commands and routes them to the
// appropriate controller or MCP tool. Single place to look up "what does
// command X do."

export const COMMANDS_CONTROLLER_PLACEHOLDER = 'commands-controller';
