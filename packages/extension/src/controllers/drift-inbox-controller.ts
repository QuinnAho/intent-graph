// Sidebar WebviewView showing the drift inbox: intent nodes whose backing
// code has drifted (signature_changed, body_semantic, moved, orphan_*, etc.).

export const DRIFT_INBOX_CONTROLLER_PLACEHOLDER = 'drift-inbox-controller';
