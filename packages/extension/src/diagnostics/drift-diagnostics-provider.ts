// DiagnosticCollection populated from skill-emitted drift events. Surfaces
// drift in the Problems panel so it lives next to compiler errors.

export const DIAGNOSTICS_DRIFT_PROVIDER_PLACEHOLDER = 'diagnostics-drift-provider';
