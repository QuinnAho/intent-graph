// Custom URI scheme `intentgraph-patch:` resolved into virtual document content
// for vscode.diff previews of proposed_patch graph nodes. Apply happens via
// WorkspaceEdit so the editor's native undo stack covers the change.

export const DIFF_PROVIDER_PROPOSED_PATCH_PLACEHOLDER = 'diff-provider-proposed-patch';
