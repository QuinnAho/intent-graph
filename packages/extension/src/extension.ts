// VS Code extension entry point. Wires controllers, transport, and skill-process
// lifecycle. Activates on `onCommand:intentgraph.openGraph`. Per Tech-Spec
// pillar 1, this file must remain a thin wiring layer — business logic lives
// in the skill subprocess.

export const EXTENSION_ENTRY_PLACEHOLDER = 'extension-entry';

export function activate(): void {
  // populated in Phase 0
}

export function deactivate(): void {
  // populated in Phase 0
}
