// Typed message envelope for vscode-messenger traffic between the extension host
// and the webview. Both sides import this file so the types stay in lockstep.

export interface MessengerEnvelopePlaceholder {
  readonly placeholder: true;
}
