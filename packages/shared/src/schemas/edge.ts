// Zod schema for the `edge` row. Edge kinds: realizes, constrains, decides,
// justifies, supersedes, contains, imports, invokes, inherits, drifted_from.
// Filled in during Phase 0.

import { z } from 'zod';

export const EdgeSchemaPlaceholder = z.object({}).describe('edge row — schema TBD in Phase 0');
