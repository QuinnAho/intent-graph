// Zod schema for the `node` row — the universal vertex in the IntentGraph.
// Concrete shape lives in Tech-Spec §4.7. Filled in during Phase 0.

import { z } from 'zod';

export const NodeSchemaPlaceholder = z.object({}).describe('node row — schema TBD in Phase 0');
