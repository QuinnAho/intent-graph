// Tier 1 of the diff ladder: normalized body hashing (whitespace, comments,
// reordering of identifier-only nodes within commutative contexts).

export const PARSER_DIFF_BODY_HASH_PLACEHOLDER = 'body-hash';
