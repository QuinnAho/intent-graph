// Tier 3 of the diff ladder: LLM-based behavior classifier. Gated, off by
// default. Calls flow through src/agent-runner — never imports `ai` directly.

export const PARSER_DIFF_LLM_CLASSIFIER_PLACEHOLDER = 'llm-classifier';
