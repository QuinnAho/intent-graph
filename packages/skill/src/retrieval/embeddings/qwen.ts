// Offline embedding client: Qwen3 via local runtime. Used when the workspace
// is in offline mode or when the user has opted out of cloud embeddings.

export const RETRIEVAL_QWEN_PLACEHOLDER = 'retrieval-qwen';
