// Read/write surface over the sqlite-vec virtual tables. One vec0 table per
// embedding-bearing kind, joined by node id (rowid).

export const RETRIEVAL_VECTOR_INDEX_PLACEHOLDER = 'retrieval-vector-index';
