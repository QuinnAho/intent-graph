// Replays event_log to reconstruct current-state tables. Verifies the hash
// chain along the way. Used for incident investigation and snapshot recovery.

export const TRACE_AUDIT_REPLAY_PLACEHOLDER = 'audit-replay';
