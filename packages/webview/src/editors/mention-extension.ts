// TipTap mention extension. `@`/`/` triggers resolve via retrieval.search;
// mentions serialize as typed pointers
// `{type:'mention', kind:'constraint', id:'auth-required'}`.

export const MENTION_EXTENSION_PLACEHOLDER = 'mention-extension';
