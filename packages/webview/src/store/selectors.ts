// Workspace-boundary barrel for narrow selectors. React Flow components and
// other consumers must import selectors here rather than reading store state
// directly — keeps re-render scope tight.

export const STORE_SELECTORS_PLACEHOLDER = 'store-selectors';
