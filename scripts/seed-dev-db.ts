// Stub. Seeds a development SQLite database with a small graph for local UI work.
// Will read /spec, build the initial node/edge set, and write through the same
// Drizzle client the skill uses at runtime. Implementation lands in Phase 0.

export const SEED_DEV_DB_PLACEHOLDER = 'seed-dev-db';
